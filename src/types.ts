export type LeaderboardGuild = {
  name: string;
  icon: string;
  conversions: number;
  createdAt: Date;
};

export type LeaderboardGuildUser = {
  name: string;
  avatarURL: string;
  conversions: number;
  createdAt: Date;
};
